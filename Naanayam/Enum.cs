
namespace Naanayam.Enum
{
    public enum Role { User, Administrator, System }

    public enum HashType { MD5, SHA1, SHA256, SHA384, SHA512 }

    public enum LogLevel { Info, Debug, Warn, Error, Fatal }

    public enum Currency { USD, AUD, INR }
}